<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Presence: the trigger is a bonded iPad being nearby. The neverForLocation flag
         means we do not have to hold location just to see a device we already paired
         with; BLUETOOTH_CONNECT lets us read the bond list and each device name. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- The watch loop is a foreground service: a BLE scan with the screen off is
         throttled to almost nothing otherwise, and the whole point is a hotspot that
         comes up while the phone is in a pocket. connectedDevice is the honest subtype. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!--
      BrightControl, so the Diagnostic screen can hand it the one line that starts Shizuku.
      Package visibility on API 30+ is opt-in: without this, asking the package manager whether
      BrightControl is installed throws even when it is, and an explicit intent aimed at it
      resolves to nothing. Named rather than a broad query, so this can see one app and no others.
    -->
    <queries>
        <package android:name="com.gios.lightcontrol" />
    </queries>

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Hotspot"
        android:supportsRtl="true"
        android:theme="@style/Theme.BrightHotspot">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Hotspot"
            android:launchMode="singleTop"
            android:theme="@style/Theme.BrightHotspot">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The presence watcher. Runs only while auto mode is on: scans for the trigger
             device, raises the AP when it appears, tears it down when no client stays. -->
        <service
            android:name=".service.WatchService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Shizuku provider: required so the app can bind the Shizuku service that
             carries the privileged tethering call. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- Re-arm the watch loop after a reboot if the user left auto mode on. -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
